{% extends 'bases/base.html.twig' %}

{% block body %}
    {% block error %}{% endblock %}

    <div class="row my-2 justify-content-sm-center">
        <div class="col-md-6 p-3">
            <div class="display-4 text-center mb-2">{{ 'sign_in_with'|trans|u.title }}</div>
            <div class="row justify-content-sm-around mt-3">
                <div class="col">
                    <a href="{{ path('connect_yandex_start') }}" class="btn btn-primary btn-lg w-100">
                        yandex
                    </a>
                </div>
                <div class="col">
                    <a href="{{ path('connect_google_start') }}" class="btn btn-primary btn-lg w-100">
                        google
                    </a>
                </div>
            </div>
        </div>
        <div class="col-md-6 p-3">
            {% block form %}{% endblock %}
        </div>
    </div>
{% endblock %}